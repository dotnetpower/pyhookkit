# Migration

Migration starts only after contract, provider parity, infrastructure, secret
handling, and rollback paths are validated.

The intended sequence is inventory, capability mapping, shadow dual-send,
canary, cutover, and rollback rehearsal.
