# T00: Visual hierarchy

Demonstrates critical content first, semantic color, concise headings, and one
focused callout without fixed-width layout.

```shell
uv run python teams_adaptive_cards/00_visual_hierarchy/teams.py
uv run python teams_adaptive_cards/00_visual_hierarchy/teams.py --send
```

The image-led card requires `EXAMPLE_ASSET_BASE_URL` for live delivery.
