# Azure Logic App Teams delivery captures

These redacted captures accompany the
[Azure Logic App Teams delivery guide](../../logic-app-teams-delivery.md).

| File | Evidence |
|---|---|
| `logic-app-workflow-expanded.png` | HTTP trigger, validation branches, Teams action, and explicit responses |
| `logic-app-trigger-security.png` | Secure trigger inputs and outputs enabled |
| `logic-app-teams-action.png` | Dynamic Team, channel, and Adaptive Card configuration |
| `logic-app-run-history.png` | Successful routed notification runs |
| `logic-app-run-success.png` | Successful trigger, validation, Teams post, and `201` response path |

The captures are cropped to the Logic Apps designer frame. Account, tenant,
subscription, callback URL, connection ID, Team ID, channel ID, run ID, and
message ID values are excluded. Do not replace them with uncropped originals.
