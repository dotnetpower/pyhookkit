# Azure Bicep

Bicep modules will define optional Logic Apps and runtime infrastructure.
OAuth-backed connector authorization remains a separate bootstrap step. The
currently verified request contract and manual runtime configuration are in the
[Logic App runbook](../logic-apps/README.md).
