# T01: Metrics dashboard

Demonstrates responsive two-column metric tiles and a compact summary suitable
for wide and narrow Teams clients.

```shell
uv run python teams_adaptive_cards/01_metrics_dashboard/teams.py
uv run python teams_adaptive_cards/01_metrics_dashboard/teams.py --send
```

The card uses only Adaptive Card 1.4 elements supported by the Workflow host.
