# T05: Progress timeline

Demonstrates completed, current, and pending steps with scannable status and
current-step emphasis.

```shell
uv run python teams_adaptive_cards/05_progress_timeline/teams.py
uv run python teams_adaptive_cards/05_progress_timeline/teams.py --send
```

The timeline is presentation-only; it does not poll or mutate deployment state.
