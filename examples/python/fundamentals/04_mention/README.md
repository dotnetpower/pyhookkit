# F04: Mention

Logical user and group aliases are resolved inside the Slack adapter. The
canonical notification contains no Slack member or user-group identifier.

The committed mappings are synthetic. A real user ID starts with `U` or `W`;
a real user-group ID starts with `S`.
